name: Multi Account Spam Bot

on:
  schedule:
    # 每20分钟运行一次
    - cron: '*/20 * * * *'
  workflow_dispatch:  # 允许手动触发

jobs:
  multi-spam-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 15  # 15分钟超时
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests ddddocr cairosvg
        
    - name: Run Multi Account Spam Bot
      run: |
        python spam_bot.py
      

